<footer class="no-print">
  Pulsa <kbd>Ctrl</kbd> + <kbd>P</kbd> para imprimir el CV.
</footer>

<style>
      @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    footer {
      background: #fdfdfd;
      border-top: 1px solid #eee;
      position: fixed;
      bottom: 0px;
      width: 100%;
      padding: 8px 8px;
      text-align: center;

      view-timeline-name: --revealing;
      view-timeline-axis: block;
      animation: linear 0.3s fadeIn;
      animation-timeline: --revealing;
      animation-range: entry 100% cover 10%;
    }

    kbd {
      background: #eee;
      border-radius: 4px;
      padding: 2px 4px;
      font-size: 12px;
    }
</style>